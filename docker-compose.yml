services:
  db:
    image: postgres:latest
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    environment:
      POSTGRES_PASSWORD: localpassword
      POSTGRES_USER: root
      POSTGRES_DB: test_challenge

volumes:
  pgdata:
    driver: local
